  while(i < 25) {                            // Can the Operating system run
      
      Left_Forward_On(1);
      Left_Forward_Off();
      Right_Forward_On(6);
      Right_Forward_Off();
      i++;

    }